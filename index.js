const num1 = 31;
const num2 = 2;
const num3 = 16;
const num4 = 6;
// multiply is an equation whose multiplied results will equal 62
let multiply = num1 * num2;

// random generates a random integer greater than 0
let random = Math.ceil(Math.random() * 8);

// mod is an equation that calculates a remainder that is equal to 4
let mod = num3 % num4;

// max will return 20 as the highest number in the set
let max = Math.max(0, 1, 5, 7, 20, 6, 2, 14, 9, 10, 18);
